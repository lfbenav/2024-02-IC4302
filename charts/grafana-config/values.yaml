prometheus: 
  namespace: monitoring
dashboards:
  prometheus-elasticsearch-exporter:
    name: prometheus-elasticsearch-exporter
    file: prometheus-elasticsearch-exporter.json
    enable: true
  elasticsearch:
    name: elasticsearch
    file: elasticsearch.json
    enable: true
  redis:
    name: redis
    file: redis.json
    enable: true
  memcached:
    name: memcached
    file: memcached.json
    enable: true
  mariadb:
    name: mariadb
    file: mariadb.json
    enable: true
  postgresql:
    name: postgresql
    file: postgresql.json
    enable: true
  mongodb:
    name: mongodb
    file: mongodb.json
    enable: true